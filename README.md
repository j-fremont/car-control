# car-control

Install the server in the Raspberry.

```
$ pip install flask flask-cors
```

Run the server.

```
[flask-server]$ python server.py
```

Install the client.

```
[car-app]$ npm install
```

Run the client.

```
[car-app]$ npm start
```
